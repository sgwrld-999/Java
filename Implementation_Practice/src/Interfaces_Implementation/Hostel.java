package Interfaces_Implementation;

public interface Hostel {
    //Both the methods declaration are same as in the interfaces
//    abstract void roomNumber();
    int roomNumber(int num);
    int numberOfPeopleInRoom(int num);
}
